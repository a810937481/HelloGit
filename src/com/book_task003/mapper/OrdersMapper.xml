<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.book_task003.mapper.OrdersMapper">
    <select id="selectOrders" parameterType="Integer" resultType="com.book_task003.po.Orders">
        select * from tb_orders where user_id=#{id}
    </select>
</mapper>